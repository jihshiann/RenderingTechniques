1. 使用的函式庫：csv, itertools, math, numpy

2. 執行:
	執行7112056073-ass03-rt-para.py
	依序輸入M, N, a1, a2, b1, b2, c1, c2, d1, d2
	輸入完成後自動執行，將在CMD介面顯示結果，並存入CSV檔

3. 其他: upper bound 設定為M*N/2，若M跟N較小(例如3*3)，可能無法在upper bound內找到peroid，此時以0顯示