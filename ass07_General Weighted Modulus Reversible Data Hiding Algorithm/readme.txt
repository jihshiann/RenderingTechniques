1. 使用的函式庫：re, csv, copy, numpy, os, cv2, pandas, random

2. 執行:
	執行7112056073-ass07-MEA，讀取各圖像(origin)以及設定(rpatab)並嵌入訊息，最後輸出3份影像至marked
	執行7112056073-ass07-EXT，讀取各圖像(marked)以及設定(rpatab)，進行解密以及保存解密後圖像(restor)和密文(mesext)，最後計算指標並存檔(imgres)